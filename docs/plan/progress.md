# progress — 進捗と注意点

作成日時: 2026-09-05 09:53
更新日時: 2026-09-06 15:58

## 感情吹き出しの画面外判定（2026-09-06 15:58）

吹き出しの最初の実装を2b5499fへコミット。続けて、カメラの投影行列と実際のウィンドウの縦横比で画面外候補を除外した。画面外の恐怖が最大8個の枠を埋め、画面内の通知を押し出す問題を防ぐ。端に一部が入る吹き出しは残し、表示期限内ならカメラを戻すと再表示する。通知の時間や戦闘計算は変更しない。

Releaseビルドと全23テスト成功（約41秒）。画面外候補による枠消費の防止、縦長・横長での判定差、カメラ移動による再表示を追加検証。既存のウィンドウサイズ変更を含むDX12テストも成功。Debugと手動操作は未検証。画像案の仕上げ、混戦時の読みやすさ、描画間で完結する状態変化の通知は継続課題。

## 感情の吹き出しを戦闘へ接続（2026-09-06 15:48）

小組の士気50以下で不安、25以下または敗走で恐怖、休息を終えて士気70以上になったときにやる気を2.5秒表示する。しきい値への復帰は不安60・恐怖35とし、通知間隔8秒で点滅を抑える。恐怖への変化は待ち時間より優先する。初期表示では通知せず、一時停止中は表示時間を止め、Homeの初期化で消去する。描画側で状態差分を観察し、戦闘計算へは書き戻さない。

32px相当の透過付き仮アイコンと吹き出しを既存アトラスの空きタイルへコードで描く。生成済み画像案は未使用。カメラに向けて小組の上へ表示し、恐怖を優先して最大8個、投影矩形の重なりを抑制する。回復以外の士気上昇、周辺敗走だけによる通知、画面外候補の除外、生成画像の透過・仕上げは未対応。描画の間に完結する状態変化は通知を取りこぼす可能性がある。

Releaseビルドと全23テスト成功（約40秒）。初期通知抑止、不安、停止中の保持、しきい値往復、恐怖の割込み、表示期限、初期化、回復と表示数制限を検証。混成戦闘の保存画像で恐怖の吹き出しを目視した。Debugと手動操作による見やすさの評価は未実施。C++プロジェクトでpackage.jsonはなく、npm run buildは対象外。
## 感情アイコンの画像試作（2026-09-06 14:56）

内蔵 image_gen でやる気（炎）・不安（汗をかいた顔）・恐怖（青ざめて震える顔）の3種類を作成し、[素材フォルダー](../../assets/ui/emotions/concepts/)へ保存した。生成プロンプトは同フォルダーの `generation-v1.json` に記録。原画は1254×1254のRGB画像で、市松模様が背景に描き込まれている。透過を指定した修正でもアルファ付き出力を得られなかったため、デザイン案として扱う。32×32への仕上げ、実際の背景透過、吹き出し枠、戦闘画面への接続は未完了。画像を目視しPNG形式とアルファの有無を確認した。コード変更はなくビルド・テストは省略。

## 感情の吹き出しを予定へ追加（2026-09-06 14:48）

部隊の状態が変化したときだけ、頭上にやる気・不安・恐怖などの小さなアイコンを短時間表示する案を[実装計画](plan.md#予定状態変化を伝える感情の吹き出し)へ追加した。前線交代・回復・撤退判断の整備に続く観察用UIとして扱う。連続通知と重なりの抑制、透過背景の小さな画像、色以外でも判別できる形を方針とした。表示機能と画像制作は未着手。今回は計画文書のみの更新のため、ビルド・テストは実行していない。

## 前衛交代と後方での立て直し（2026-09-06 14:45）

ユーザーの「消耗した前衛が一度引き、予備が前進する。全体が消耗してから撤退を検討する」という希望に合わせ、既存の前線両端の交代を維持した。交代開始を兵力50%以下→75%以下、士気30以下→50以下へ早めた。疲労条件は8のまま。左右にずれた備でも交代を検討し、衝突・経路条件は維持する。弓を近接の交代予備や回り込みへ出さない。

交代して後方へ下がった小組は休息し、敵から10以上離れ、直近3秒間被害がなく、周囲の敗走による動揺がなければ士気を毎秒3回復、疲労を追加で毎秒1回復する。士気70以上・疲労2以下で休息を終える。兵力は回復しない。交代後退中・休息中は士気10超かつ兵力25%超なら、士気20以下になっただけで敗走しない。

複数の敗走源から同時に受ける動揺は合計毎秒6までに制限。全体撤退は「敗走13小組以上」「戦力に余裕のある小組が5以下」が6秒続いてから確定する。余裕とは非敗走・非休息で、兵力50%超、士気40超、疲労8未満。状態が改善した場合は検討時間を毎秒2戻す。画面タイトルへ余裕のある小組数と全体撤退の検討状態、選択小組へ再集結中の表示を追加した。

交代経路は前線両端のみ。中央三列の交代、全軍で一時後退して再布陣する動作、プレイヤーへの撤退選択UIは未実装。休息完了は後方での待機復帰であり、全員が必ず前線へ戻れることを保証しない。

Releaseビルドと全22テスト成功（約39秒）。Debug関連3テスト（休息・撤退、戦闘、個体表示）成功（約50秒）。交代中の士気18での継続、士気10での敗走、安全な後方での回復、健全部隊が残る場合の継続、消耗継続による全体撤退、左右対称・陣営反転・固定刻みと既存の交代・回り込みを確認した。手動操作と戦闘バランスの評価は未実施。

## 赤青で異なる初期布陣（2026-09-06 14:31）

ユーザー希望により、F8の混成戦闘の赤軍を中央の槍・両翼の刀、青軍を前二列と第三列両端の槍・第三列中央と第四列の刀予備に変更。弓は両軍とも最後列。各軍の兵力は槍240人・刀160人・弓100人を維持する。開始位置と進軍先のXを赤-6・青+6にずらし、正面の組み合わせを変える。史実の陣形名を付けた再現ではなく試作の布陣。Homeでもこの配置へ戻る。

Releaseビルドと全21テスト成功（約39秒）。左右の開始位置、赤青の前衛・予備の兵種、射撃・交戦と操作を確認。操作テストの固定座標を現在の初期位置・小組位置から求める形へ修正した。今回の布陣変更後のDebug再検証と手動操作は未実施。


## 弓を混成戦闘へ接続（2026-09-06 14:14）

前段の同時攻撃・槍刀混成を `cbffebf` にコミットした。続けて弓の射程・向き・射撃間隔・曲射・着弾損害を追加し、F8の混成を槍240人・刀160人・弓100人ずつへ変更した。矢は発射時の狙点へ飛び、移動した敵に外れる。味方と地形で射線を確認し、飛翔中の遮蔽でも停止する。射手の敗走後も発射済みの矢は残り、一時停止・初期化に対応する。

三兵種の同時表示と、代表の矢三本による飛翔表示、着弾した小組への死亡表示を接続した。接敵後に備の移動要求フラグが残るため射撃できなかった問題は、実座標の変化で停止を判定するよう修正した。素材確認画面には矢用の描画領域を触れないようにした。[弓の仕様](../reference/ranged_combat.md)を参照。

Release全21テスト成功（約39秒）。Debug関連6テスト（射撃、兵種、混成描画、素材、戦闘、個体表示）が成功（約114秒）。混成描画で斉射69回・着弾57回、飛翔中の矢の表示を確認し、DirectXの警告・エラーは0件。保存した交戦画像で三兵種と両軍の矢を目視した。手動操作・戦闘バランスは未検証。次は騎馬の機動・突撃、弓の退避、面内の配置と混雑時の見た目を進める。

## 複数相手への同時攻撃と槍・刀混成（2026-09-06 13:55）

面ごとの配置済み人数を相手別に集計し、射程と遮蔽を満たす複数相手へ同じ刻みで損害を与えるようにした。残存兵力・幅による上限と毎秒2人の再配置を維持。主対象は移動・小組の旋回に使い、主対象へ向き直ってから各面の兵が戦う近似とする。個体表示も参加対象から相手を選び、相手別の参加割合を表示人数の上限にする。同一個体を複数相手へ重複投入しない。

F8 / `--mixed-battle` で槍足軽300人・刀武士200人ずつの500対500を追加。中央三列が槍、両翼各一列が刀。兵種は小組に保持し、射程・旋回・個体の移動と図柄に反映する。Homeで混成編成を維持して初期化。備への移動・停止命令は従来通り。弓・騎馬は未参加で、専用の射撃・突撃実装後に追加する。

Release全20テスト成功（約37秒）。Debugは関連5テスト（兵種、接触面、戦闘、個体表示、混成描画）が成功。正面＋背面、正面＋側面、同じ面の複数相手への同時損害、兵力保存、更新間隔の一致、兵種別射程、混成戦闘の描画を確認。交戦画像で槍と刀の混在を目視した。F8・Homeの入力と両兵種のAtlas領域も検証した。Debug戦闘テストは多数の状態コピーにより既定スタックを超えたため、テスト実行ファイルのみ4MiBへ拡張して再実行し成功（約19秒）。同じ面の二相手を互いに重ならない配置にした検証もRelease / Debugで成功。手動操作・バランス調整は未実施。

次は参加兵の面内配置と旋回、混雑時の見やすさを改善し、弓の射撃と騎馬の突撃を混成戦闘へ接続する。

## 接触面の参加人数を損害へ接続（2026-09-06 13:21）

シミュレーション優先と共通時計を `3f796de` にコミットした。続けて各面の配置済み人数と有効幅から相手別の参加上限を求め、既存の攻撃対象への損害を人数に比例させた。配置前の予備は攻撃せず、同じ接触幅に予備を増やしても攻撃力は増えない。選択タイトルへ攻撃参加人数を表示する。複数相手への同時攻撃と表示兵士との参加人数の同期は未実装。

投入開始が遅れるため、極端に消耗した前列が敵の損害を受ける前に敗走し、端数兵力を残す場合がある。損害の小組間への流出がないことを、その生存分も含めて検証する。通常の槍戦闘では前列交代より敗走が先行する展開になり、交代自体は専用配置の単体テストで検証する。

Debug / Release各19テスト成功。未配置時の攻撃抑止、配置人数による実損害の差、予備追加での損害不変、接触幅の上限、複数面の兵力保存、既存の方向補正・敗走・交代と描画を検証した。全テスト所要時間はRelease約31秒、Debug約175秒。手動操作・戦闘バランスの調整は未実施。

## シミュレーション優先と共通時計（2026-09-06 12:24）

ユーザー指示により、自律シミュレーションを最優先、関ヶ原の時系列再生を後順位に変更した。通常起動は槍の戦闘試作、F7で刀の戦闘。既存の関ヶ原デモは `--sekigahara` で使用できる。

`BattleClock` を追加し、小組（内部60Hz）と個体の移動・旋回・衝突・攻撃を共通の30Hz刻みで進めるよう通常実行と戦闘描画テストへ接続した。30fpsと144fps相当で全個体の状態一致、一時停止と初期化時の端数破棄を検証する。表示位置から小組損害への書き戻し、接触面配分の実戦闘への反映は未実装。

Debug / Release各19テスト成功。全テスト所要時間はRelease約28秒、Debug約154秒。戦闘描画テストも個体を30Hzで計算するため、従来の10Hz検証より計算量が増えた。戦闘2テストのタイムアウトは120秒へ変更。通常起動の画面を手動で観察する検証は未実施。

## コリジョンの補正速度と近隣探索（2026-09-06 12:15）

簡易コリジョンを `fd2b6d6` にコミットした。続けて補正量を `min(0.11, 6.6 × 経過秒)` に制限し、短い更新間隔で過剰に押し戻されることを抑えた。近隣探索は格子ごとのvectorからIDの連結リストへ変更し、配列確保と非接触時の平方根計算を減らした。固定刻みと多数の兵士の軌跡の更新頻度への非依存は未実装。

Debug / Release各19テスト成功。30 / 60 / 120 / 240Hz相当で補正速度上限・分離への収束・経過時間0での停止を確認した。全テスト所要時間はRelease約19秒、Debug約93秒（前回約21秒・101秒）。これは単回のテスト実行時間であり、ゲームのFPSの測定ではない。手動での混雑時の観察は未実施。

## 戦闘表示の簡易円コリジョン（2026-09-06 12:09）

前進方向の改善を `1ffd117` にコミットした。続けて固定IDの表示兵士へ近隣格子による重なり補正を追加した。生存者の中心間隔0.22未満を双方へ押し戻し、一体の補正は一更新0.11まで。攻撃対象は補正後の位置で選ぶ。死亡者を動かさず、描画人数や小組単位の戦闘結果には依存させない。実兵の体格や完全な非重複の保証ではなく、表示密度に合わせた試作。

Debug / Release各19テスト成功。二体の対称分離、完全一致からの分離、分離後の停止、死亡者の位置保持、既存の接敵・命中・敗走と描画を確認した。全テスト所要時間はRelease約21秒、Debug約101秒。手動での混雑時の観察と実時間の性能測定、連続衝突判定は未実施。

## 体の向きに沿った個体移動（2026-09-06 11:44）

加減速・旋回の改善を `759ce1d` にコミットした。続けて表示兵士の移動方向を体の正面へ制限し、背後への持ち場変更ではその場で旋回するようにした。向きの一致度に応じた前進、到着時の停止、退路開放後の旋回と逃走を検証する。個体コリジョンと固定刻みの統合は未実装。

Debug / Release各19テスト成功。槍・刀の背後への方向転換、横滑り・後退・持ち場の通り越しの抑止、復帰と退路開放後の歩行再開を確認した。既存テストは旋回時間と継続的な表示更新を含む条件へ変更した。手動での動きの観察は未実施。

## 戦闘中の個体旋回（2026-09-06 10:03）

槍・刀の表示兵士へ兵種別の旋回速度を適用した。移動・整列・接敵時の瞬時の向き変更をなくし、敵への角度差が約20度以内になってから攻撃表示を始める。小組単位の損害計算は維持し、個体衝突と移動方向の統合は継続課題。

Debug / Release各19テスト成功。移動中・戦闘中の旋回上限、攻撃時の向き、背を向けた個体の攻撃抑止を確認した。既存の四方向の命中時刻テストは、正面を向いた初期条件を明示した。手動での動きの観察は未実施。

## 戦闘中の個体追従の加減速（2026-09-06 09:49）

刀武士の近接戦闘を `58a8158` にコミットした。続けて槍・刀の表示兵士に追従速度を持たせ、隊列確認と共通の兵種別加速値を適用した。離れた持ち場への追従速度に上限を設け、到着時は減速・整列する。損害・士気は既存の小組計算を維持する。個体旋回・コリジョンと隊列モデル全体の統合は継続課題。

Debug / Release各19テスト成功。持ち場を10単位離し、移動量・加速上限・再整列・一時停止・初期化を槍と刀で確認した。刀・槍の交戦と局所敗走の描画テストも成功。手動での動きの観察は未実施。

## 刀武士の近接戦闘（2026-09-06 09:40）

隊列確認を `20ea1b2` にコミットした。続けて刀武士を既存の小組戦闘へ接続し、F7 / `--sword-battle` を追加。槍より短い間合いで接近・斬撃し、損害・局所敗走・全体撤退を更新する。[仕様と制限](../reference/melee_profiles.md)を参照。24体の隊列移動との統合と異兵種混成は未実装。

Debug / Release各19テスト成功。射程・向き・攻撃線の閉塞、個体表示の間合い、更新頻度と描画を確認し、交戦中画像を目視した。

## 兵種別の隊列移動（2026-09-06 08:56）

4列×6段・24体の隊列確認を追加。素材確認からF6、または --formation で起動できる。兵種別の間隔・速度・加速・旋回、各兵の持ち場への追従、円コリジョン、到着時の減速と再整列、移動命令・停止・兵種切り替えを実装した。[操作と仮ルール](../reference/formation_drill.md)を参照。自律戦闘や既存の軍勢階層への統合は未実装。

Debug / Release各17テストが成功。4兵種の進路変更・到達・再整列・間隔と、入力・GPU描画を確認し、騎馬隊列の旋回中画像を目視した。停止付近の向き調整後は対象テストを再検証した。

## 追加兵種の素材と確認画面（2026-09-06）

刀武士・弓足軽・騎馬武者のモデル生成、待機8方向と移動・攻撃各8方向×8コマを追加した。合計408コマのPNGと生成条件JSONを同梱し、通常ビルドではBlender不要。関ヶ原の「兵種を見る」から確認画面へ入り、F4で槍足軽を含む4兵種を切り替える。F3で移動／攻撃、Spaceで再生／停止、Homeで初期化する。騎馬用の表示寸法と足元Pivotを追加し、攻撃から移動へ戻した際も寸法を復元する。

Debug / Release各15テスト成功。408コマの透過・画像端・動作差、描画・兵種切り替えと既存戦闘への回帰を確認した。生成スクリプトのPython構文確認も成功。兵種の射撃・突撃・固有能力・専用音声、関ヶ原の史実編成への割当は未実装で、新兵種は素材確認用。関ヶ原と従来の戦闘は引き続き槍足軽表示を使う。

## 関ヶ原の史実再生デモ（2026-09-06）

ユーザー指定に合わせ、初期コンテンツ領域を1920×1080へ変更した。モニターDPIに応じて枠を調整し、起動時にクライアント領域の実寸を確認する。

主要14軍勢、丘陵6地点を持つ模式地形、進軍矢印、陣営・転進色、7局面の説明を追加。08:00〜14:30の再生・停止・速度変更・時刻ドラッグ・巻き戻し・軍勢選択・俯瞰カメラを実装した。通常起動は関ヶ原デモになり、右上のボタンまたは `--battle` から既存の戦闘試作を使用できる。

純粋な再生データと時刻サンプルを戦闘計算から分離。現段階は通説を説明する模式デモで、自律戦闘・介入・実標高・史実兵数・全軍勢の再現は未実装。再生は無音。採用した資料、推定と省略、操作、将来の比較・介入への実装順を[関ヶ原デモ資料](../reference/sekigahara_demo.md)にまとめた。

Debug / Release各11テストで巻き戻し、転進境界、再生操作とGPU描画、既存戦闘への回帰を確認。初期布陣・転進中のUI付き画像を目視し、旗ラベルと移動矢印を調整した。実マウスによる手動操作、低解像度・高DPI、長時間稼働は未確認。

## 動揺の観察表示（2026-09-05 14:51）

備ごとの動揺中の小組数、選択小組の近隣敗走数と士気低下量（毎秒）、動揺源の残り秒数をタイトルに追加した。判定は戦闘計算と共通化し、交代後の区画・期限切れ・戦闘終了に対応する。

追加後のDebugは8テスト成功。続くRelease検証中に同じ作業ツリーの戦闘・移動処理へ別の変更が入ったことを確認し、battle_simulation、scene_invariants、dx12_combatが失敗した。この検証時点では統合状態の確認が未完了だった。今回、待機理由と敵選択の修正を含む統合状態でDebug / Release各8テストの成功を確認した。タイトルの実画面での読みやすさは未確認。

## 小組の簡易迂回（2026-09-05 15:24）

射程外への直進が塞がれた場合、左右の中継点から通れる経路を選ぶ。通行判定は移動線分全体に適用し、迂回先も塞がれれば待機する。選択タイトルへ「迂回中」を追加。Debug / Release各8テストが成功し、障害物を回って接敵すること、移動中の間隔、閉塞時の待機、一時停止、前列交代との共存を確認した。複雑な経路探索は未実装。敗走者の退路閉塞は下記の追加で対応した。

## 敗走者のコリジョン（2026-09-05 16:15）

敗走小組は味方・敵・他の敗走者を避けて進み、退路が塞がれれば停止し、空けば再開する。選択タイトルに「退路閉塞」を追加。通常の小組接近・交代も敗走者を障害物として扱う。戦闘終了時は前列交代予約を解除する。この時点では備全体の移動との衝突統合は未実装だったが、下記で追加した。追撃による損害と複雑な退路探索は未実装。

Debug / Release各8テストで、味方・敵の回避、閉じた退路、出口の再開、敗走者同士の間隔、固定刻み、一時停止とH、歩行表示の停止・再開を検証した。Releaseはbuild/verify-group-lossで検証。

## 備全体の移動と小組衝突（2026-09-05 16:24）

命令移動・全体撤退・備同士の押し戻しを小組中心の移動線分で検証する。敵の生存小組と自軍の敗走者を障害物として扱い、両軍同時移動と相手の停止を考慮してから変位を確定する。閉塞時は備を停止し、命令先を保持して通路が開けば再開する。タイトルに「備の進路閉塞」を追加。備単位の自動迂回は下記で追加した。分割通過は未実装。

Debug / Release各8テストが成功。敵小組・両軍の敗走者による閉塞と再開、高速移動のすり抜け防止、同時接近、押し戻し時の後方障害物、勝敗確定後の撤退、一時停止・H・初期化を検証した。撤退先への到着が閉塞で遅れるため、描画スモークの戦闘終了条件は敗北状態で判定する。

## 備全体の簡易迂回（2026-09-05 17:02）

閉塞した備が、必要なら後退して左右へ避け、障害物を通過して元の目的地へ戻る経路を選ぶ。全小組について経路の線分を検証し、途中の障害物変化でも再判定する。H・Move変更・全体撤退では旧経路を破棄し、Spaceは保持する。タイトルに「備の迂回中」を追加した。

Debug / Release各8テストが成功。25小組の備が敗走者を避けて目的地へ到着すること、通過中の間隔、停止命令・再命令・一時停止・固定刻み・初期化を確認した。目的地占有などで候補が通れない場合は待機する。複雑な迷路と分割通過は未実装。

## 実距離に基づく動揺（2026-09-05 22:16）

動揺の伝播を配置区画から現在の小組中心間距離へ変更した。同じ備の半径8以内が対象で、影響量は1敗走小組につき毎秒6、寿命8秒を維持する。元の隣接区画でも遠ければ影響せず、別区画でも近ければ影響する。Debug / Release各8テストが成功し、距離境界、斜めの範囲外、逃走位置と受け手の変位、局所敗走連鎖を確認した。

直前の局所戦闘・コリジョン・迂回までを6e8a825にコミット済み。この追加はその後の変更。

## 動揺範囲の可視化（2026-09-05 22:29）

選択した敗走小組の影響範囲を黄色、動揺を受ける側の周辺範囲を水色で描く。関連する影響先・動揺源は橙色の小円で示し、タイトルに凡例を追加した。影響判定はSimulationと共通化。選択解除・期限切れ・戦闘終了で消える。

Debug / Release各8テストが成功。範囲半径、対象数と戦闘判定の一致、選択変更と表示解除、固定描画数を検証。描画スモークで動揺源を選び、build/rout-overlay.pngの保存画像で黄色の範囲円と橙色の対象表示を目視した。

## 接触面と包囲戦の方針整理（2026-09-05 22:36）

[接触面・兵力配分・包囲戦](../reference/contact_fronts.md)に、ユーザーとの議論、現行方式との差分、初期モデル案、実装順序、合格条件を記録した。同じ接触面の容量、全方向の兵力保存、配置転換の時間、混雑と交代、退路閉塞を重視する。側背面の仮倍率との二重計上を避け、内線・外線の用語と長期補給の時間尺度を区別する。

直近の実装は接触面の幾何・相手・幅の計算と検証から開始する。面別の攻撃配分・損害計算はまだ未実装。今回は文書のみを変更し、文書間リンクと差分を確認した。実行コードを変更していないためビルド・CTestは再実行していない。

## 接触面の観察用試算（2026-09-05 22:45）

小組の4面を16区間ずつに分け、届く相手と面ごとの幅を読み取り専用で求める処理を追加。各区間を最近の敵一つへ割り当て、幅の重複加算を防ぐ。選択タイトルに「接触幅(試算)」を表示する。既存の損害・行動・兵士攻撃には反映していない。

Debug / Release各9テストが成功。正面、横ずれ、射程外、遮蔽、前後同時接触、複数敵、回転・平行移動・両軍交換を確認した。有限区間による近似と円形占有領域との違いを設計資料へ記録。次は幾何の追加検証、参加上限、全方向の兵力保存へ進む。

## 面別の参加人数・予備の配分案（2026-09-05 23:52）

接触幅0.9あたり1人、一列の仮設定で面別・相手別の参加人数を求める。人数が不足する場合は容量比で配分し、余剰は予備に残す。選択タイトルに面別の参加案と予備案を表示。現在の条件に対する読み取り専用の配分であり、実配置・攻撃人数ではない。次は有限時間の再配置を加え、損害と表示へ連動させる。

Debug / Release各9テストが成功。幅の上限、残存兵力と予備の保存、前後二面、複数敵、小数・ゼロ兵力、非接敵と面順序の交換を検証した。

## 面別再配置の時間更新（2026-09-06 00:06）

直前の動揺表示・接触面・配分案までを46e2cd1にコミットした。その後、小組ごとの面別配置状態を追加。毎秒2人を全体上限に予備へ戻し、残りの枠と元からある予備で不足面を補う。同じ刻みで別面へ瞬間移動しない。損失で配置人数を縮小し、敗走時は解除する。

タイトルに現在→目標の人数と予備を表示。Debug / Release各9テストが成功し、前後の再配置・保存則・損失・離脱・敗走・一時停止・固定刻みを検証した。損害と兵士表示は従来方式のままで、次は実参加人数への接続が必要。

## 現在の状態

Visual Prototypeに水平360°のカメラ回転、2部隊の進軍と選択・移動・停止命令を追加した段階。C++20 / CMake / DirectX 12で、起伏のある3D地形とBlender製の8方向Idle / Walk Spriteを表示できる。Spaceで進軍・一時停止、Homeで初期配置へ戻せる。接触戦闘・損害・隊列と士気の低下・自動撤退を実装し、500対500の交戦から敗走まで観察できる。接敵する前列だけが槍を突き、後列は待機・欠員補充する。

技術仕様の構造体・JSONファイル名・Blenderスクリプト名は引き続き設計例。現在の実ファイルと役割は[開発手順](../reference/development.md)に記載した。

## 完了した整理

- 技術仕様 Draft 0.1を読み、目的・設計原則・Prototype 01の到達点を[goals.md](goals.md)に整理した。
- 設計書の優先順位に沿って、実装予定と確認項目を[plan.md](plan.md)に整理した。
- [ドキュメント入口](../README.md)と[設計概要](../reference/architecture.md)を追加した。
- 元の技術仕様と参考画像の配置を維持した。
- [音響方針と必要素材リスト](../reference/audio_assets.md)を追加。こちらで制作できる仮音、外部素材・収録の候補、優先度・数量・長さ・受け渡し形式を整理した。風・進軍・交戦の仮音合成とゲーム内再生を追加済み。本素材の制作・調達は未着手。

## 未着手

- 任意方向・障害物に対応する回り込みと、個体の命中から部隊損害を決める戦闘判定。現在は敵兵が近い生存前列のみ死亡対象とし、対象不足なら後列を倒さない。
- 専用の死亡動作の素材、個体間の重なりの軽減、個体単位の戦闘判定。
- 正式な性能測定、フレーム並列化、移動中の継続的な画質評価。
- Prototype 01の攻撃表現・戦況の読みやすさ・戦闘バランスの調整。
- 追加4兵種の実装、歩行と素材品質の調整。
- 関ヶ原の地形・軍勢・イベント、Effects / Audio / UI。

Blender 5.2.1 LTSで槍足軽の6ボーンRigとSkinning、8方向×8フレームの歩行Spriteを生成し、DX12へ組み込み済み。次は[ビジュアル方針](../reference/visual_direction.md)に沿って歩行の見た目を調整し、槍突きと戦況の見やすさを調整する。今回のモデルは試験素材であり、本素材の完成ではない。

## 編成の階層化

[4階層の編制](../reference/organization.md)を追加。各備500人を25小組・5組・2隊へ分け、小組の間隔と所属内での欠員補充を実装した。小組ごとの待機・前進・交戦・撤退状態を保持する。正面接敵時に前から2列目の左右端2小組が外側を経由して前進する簡易経路を追加。Hで中断し、通路が塞がれると待機する。離脱・戦闘終了時の標準配置への復帰と、疲労した回り込み小組が帰還してから予備小組を出す交代を実装した。回り込み中も小組を選択し、所属する隊・組・疲労・状態をタイトルで確認できる。正面両端の小組と直後の予備の交代を実装した。小組士気の平均と敗走数を備へ集計し、局所敗走から全体撤退への連鎖を追加した。中央3小組の交代と隊・組単位の選択UIは未実装。

## 実装済み

- 塞がれた最短の敵へ固執せず、攻撃線・接近経路の空いた敵を優先して選択。選択小組に対象なし・射程外・旋回中・攻撃線の遮蔽・進路閉塞・停止命令の理由を表示し、接敵と攻撃も区別する。

- [小組単位の接近・方角・コリジョン](../reference/local_combat.md)：実距離16未満の敵を選び、空いている経路で射程へ接近する。毎秒180度で旋回し、距離7.5・正面±60度・攻撃経路の遮蔽から損害を決める。側面1.25倍・背後1.5倍。小組の攻撃力だけを選択相手へ適用し、表示兵士もその相手の方向へ攻撃する。

- 小組の士気20以下・兵力25%以下で個別敗走し、現在位置から半径8以内の同じ備の小組へ最大8秒間の動揺を与える。孤立した1小組の敗走は健全な近隣が耐えられ、低士気や複数の敗走で連鎖が進む。13/25小組の敗走で備全体が撤退する。敗走者は攻撃・予備選択から外れ、Move／Hでは引き戻されない。タイトルに敗走小組数と選択小組の敗走状態を表示する。

- 正面両端の疲労・消耗した小組を直後の予備と交代する。外側へ退避、前列の後退、予備の前進、横移動の順に進み、所属・損害・表示IDを保持して配置だけを交換。選択タイトルに前列交代中／交代後退中を表示する。

- 死亡表示の上限を小組ごとの損失割合と所属表示ID数から求める。命中した生存前列だけを対象とし、無傷の小組へ死亡を流さない。繰り返し命中でも既存の死亡数を含めて上限を守る。

- 小組の残存兵力と士気を追加し、選択時のタイトルに表示。射程と方向条件を満たした小組の攻撃を選択相手へ適用し、実際に減った兵力だけ備から減らす。両翼は兵力半分以下・士気30以下でも帰還し、その小組を予備に選ばない。

- 風、足音・装備の擦れ、槍の衝突を模した仮音を起動時に合成し、XAudio2で再生する。兵士の移動と槍の命中タイミングに追従し、カメラに応じた左右定位・距離とZoomの減衰、M消音とF5・＋／－による全体・環境・効果音の音量調整に対応。一時停止・素材確認・フォーカス喪失・最小化時は消音。実機での試聴は未実施。

- 生存前列の個別対象選択と8方向×8フレームの槍突き、後列の待機・欠員補充。F3で素材確認の歩行／槍突きを切り替える。[基本5兵種の計画](../reference/unit_types.md)を整理した。

- [兵士ごとの表示状態](../reference/individual_soldiers.md)：個体差のある隊列追従・動作時計、生存→倒れる途中→倒れた状態、死亡位置の保持。カメラ回転・素材や表示人数の切り替えでも状態を保持し、Homeで全員を初期化。倒れ方は既存Spriteを地面へ傾ける仮表現。
- [接触戦闘・士気・撤退](../reference/combat_prototype.md)：1/60秒の部隊更新、接触時の押し戻し、損害・隊列・士気の低下、崩壊と勝敗、敗走先への移動。欠員と隊列の乱れを表示し、タイトルに両軍の状態を表示。

- 兜と軍旗の[アプリアイコン](../reference/app_icon.md)を実行ファイルへ埋め込み、ウィンドウの大小アイコンに設定。
- 左クリックで赤・青の部隊を選択し、色を明るく表示。空き地で選択解除、右クリックで移動先を指定、Hで選択部隊を停止。停止中は命令を保持し、Spaceで再開する。地形の三角形との交差で移動先を求め、隊列が地形内に収まるよう中心を±60に制限する。

- CMakeとPowerShellによるDebug / Releaseビルド、CTest。
- Win32ウィンドウ、俯角固定・水平360°回転のOrthographic Camera、WASD / 矢印による画面基準のPan、ホイールZoom、リサイズ。Q / Eの長押しと中ボタンドラッグで連続回転。キー解放・フォーカス喪失でキー回転を停止する。
- ユーザーの希望に合わせ、初期コンテンツ領域と起動テストの保存画像を1920×1080へ変更。DPIを考慮した枠調整とクライアント領域の検証を追加。
- 起伏を持つ地形、黄土色と緑の地表、64×64タイルを12列×17行に並べたAtlas。
- Blenderによる槍足軽の8方向Idle / Walk Sprite生成、減色・透過処理、WICによるPNG読み込み、足元Pivotを使った配置。
- F2の素材確認画面、Vの仮素材比較、Z / Cの兵士方向切り替え、Spaceの歩行再生。
- 表示数と独立した2部隊の移動、停止・再開・初期化、目的地での停止。兵士の相対位置を維持し、地形へ接地させながら歩行を再生。
- 赤・青の兵士、旗、松、陣幕。1 / 2 / 3キーで1,000 / 5,000 / 10,000体を切り替え。
- GPU Instancing、透過部分の破棄、深度テスト、Texture転送、Fence同期。
- 実描画のBMP保存、DX12デバッグメッセージ検査、Scene / Camera / Simulationの単体テスト。

## 未決事項・注意点

- ユーザーの希望を受け、原設計の「カメラ回転なし」を水平回転可能へ変更した。俯角は固定。木・旗・陣幕は1方向の仮素材なのでカメラに向く簡易表示。
- 設計書はDraft 0.1であり、Zoom方式、Sprite解像度・方向数、追加Buffer、将来のゲームモードには候補や検討事項が含まれる。
- 1,000 / 5,000 / 10,000体の短時間の描画起動は確認した。60fps程度の継続達成や正式な性能実績は未検証。毎フレームGPU完了を待つ最小構成で、デバッグレイヤーとVSyncも有効。
- Prototype 01の兵士表示数とFormationの兵力は別の概念。仕様中の兵力1,000に対して100～200 Spriteを描く例も固定比率ではない。
- 戦闘計算式・固定更新・崩壊時の勝敗は試作ルールを採用。データスキーマ、経路探索、多部隊の衝突、追撃・再集結は未実装。初期士気は観察用に赤100・青85としている。
- 設計書23節のエージェント向け設計原則は、今回[goals.md](goals.md)に整理した。ルートのエージェント設定ファイルは今回変更していない。
- バージョンの基準とされる`package.json`は未作成。Draft 0.1は文書の版であり、アプリのリリース版を意味しない。
- 元の資料に作成日時の記載がなかったため、今回編集した既存文書の「作成日時」は文書管理上の初回記録日時とした。原資料の実際の作成日時は不明。

## 命中と死亡表示の連動

槍突きの4コマ目（0始まり）を通過した攻撃の相手だけを死亡候補にする。射程内の生存前列・小組別の損害による上限を維持し、複数命中を重複計上しない。部隊損害自体は従来の集団計算であり、個体の命中からは集計しない。

## 今回の検証

- 2026-09-05 15:11：待機理由の表示と通行可能な敵の優先選択を追加。Debug / Releaseの各8テストが成功。攻撃線の塞がれた最短の敵から別の敵への切り替え、遮蔽と進路閉塞の理由、固定刻みの一致を検証。Releaseはbuild/verify-group-lossを使用。タイトル表示の実画面での読みやすさは未確認。

- 2026-09-05 15:03：局所接近・方角・簡易コリジョンを追加し、Debug / Releaseの各8テストが成功。8方向の接近・間隔保持、円形射程外、備の接触外での小組攻撃、旋回の遅れ、側面／背後の損害差、攻撃線と進路の遮蔽、固定刻みの一致を検証。交戦画像を目視。Releaseはbuild/verify-group-lossで検証。

- 2026-09-05 14:43：局所敗走と士気連鎖を追加し、Debug / Releaseの各8テストが成功。孤立した敗走への耐性、同一刻みの再帰抑止、近隣への段階的な波及、過半数による全体撤退、30／144fps・一括更新の一致、命令から独立した逃走、交代予約の解除、逃走者の攻撃停止と残存小組の攻撃継続を検証。局所敗走の保存画像を目視。Releaseはbuild/verify-group-lossで検証。

- 2026-09-05 14:27：正面両端の交代を追加し、Debug / Releaseの各8テストが成功。四方向の区画交換・小組間隔・所属維持・疲労回復、H／Space／Home、離脱・戦闘終了、予備の適性と通路閉塞、表示兵士の位置保持を検証。通常の戦闘描画テストでも区画交換の完了を確認し、交戦中の保存画像を目視。Releaseは既存の検証用build/verify-group-lossを使用。

- 2026-09-05 14:06：小組別の死亡表示をDebug / Releaseの各8テストで検証。四方向の命中対象、無傷小組への死亡流出抑止、繰り返し命中時の上限、離脱・一時停止・密度変更・初期化を確認。通常のRelease実行ファイルが起動中のため、Releaseはbuild/verify-group-lossでビルド・検証した。
- 2026-09-05 13:50：Debug / Releaseの各8テストが成功。接敵小組の損害・士気低下、予備の無傷、備との兵力合計、全滅前列から後列への被害流出抑止、兵力・士気不足の予備除外、更新間隔による一致を確認。

- 2026-09-05 13:23：Debug / Releaseの各8テストが成功。音量の保存・再読込・上書き、不正設定の無視、Win32入力で回り込み小組の選択・所属表示・備への命令・Homeでの選択解除を確認。

- 2026-09-05 13:18：Debug / Releaseの各8テストが成功。全体音量の適用、環境音と効果音の独立調整、0〜100％の制限、音量変更時の定位保持を検証。音量設定の永続保存と実際の試聴は未実施。

- 2026-09-05 13:05：Debug / Releaseの各8テストが成功。槍突きの最大伸長での命中通知、再描画による重複抑止、再生頻度の制限、消音・初期化後の遅延音抑止、短い衝突波形を確認。試聴による品質評価は未実施。

- 2026-09-05 13:00：Debug / Releaseの各8テストが成功。波形長・ループ端・クリッピング、部隊動作によるミックス、一時停止・消音、カメラ回転とZoom、大量兵士でも音量が増幅しないことを検証。音声デバイスの無音での初期化に成功。

- 2026-09-05 12:42：Debug / Releaseの各7テストが成功。両軸で疲労による帰還→予備の出発、復帰経路の順序、休息での疲労回復、離脱時の標準配置への復帰、復帰中のSpace・H停止、兵士表示の追従を確認。

- 2026-09-05 12:35：Debug / Releaseの各7テストが成功。X/Z方向の回り込み、通路の占有、一時停止・H停止・初期化、固定刻みの一致、兵士の小組追従を確認。交戦画像で両翼への展開を目視した。

- 2026-09-05 12:27：Debug / Releaseの各7テストが成功。四方向で命中前の死亡抑止、攻撃対象と死亡対象の一致、同時刻の再描画、離脱後の死亡抑止を確認。

- 2026-09-05 12:15：階層化後のDebug / Releaseで各7テストが成功。所属と兵力合計、前方5小組の交戦・後方20小組の待機、小組内補充を検証。保存画像で小組間の隙間を確認。

- 2026-09-05 12:09：Debug / Releaseの各7テストが成功。前列だけの攻撃・射程・対象、後列の待機と補充、攻撃画像の透過・端・ポーズ差を確認。10,000体・WARPと仮素材の戦闘描画も成功し、DX12警告・エラーは0件。Blender生成とPython構文確認が成功。

- 2026-09-05 11:52：損害対象を接敵する生存前列へ限定。Debug / Releaseの各6テストが成功し、四方向の前列選択・遠方の敵による死亡抑止と、接敵線に残る兵士の画像を確認。
- 2026-09-05 11:41：個体表示追加後のDebug / Releaseで各6テストが成功。追従の個体差、停止中の死亡動作、死亡位置・描画軸の保持、表示人数切り替えと初期化を確認。10,000体・WARPと仮素材でも戦闘描画が成功し、撤退後に残る兵士を目視。
- 2026-09-05 11:29：Debug / Releaseで各6件のCTestが成功。更新頻度に依存しない戦闘、対称条件と逆条件の勝敗、停止・離脱・撤退・初期化、表示数に応じた欠員を検証。80秒の戦闘描画は10,000体・WARPでも成功し、DX12警告・エラーは0件。交戦中と敗走後の画像を目視。

- 2026-09-05 11:18：アイコン組み込み後のDebug / Releaseビルドと各4件のCTestが成功。埋め込みResourceの大小アイコン読み込みと、Release実行ファイルから抽出した32pxアイコンの図柄を確認。
- 2026-09-05 11:12：Debug / Releaseのビルドと各4件のCTestが成功。移動命令・一時停止中の保持・到達・停止・地形内への制限、回転・ズーム・Pan後の地形クリック、Win32メッセージによる両部隊の選択・移動・停止・選択解除を検証。実マウスの手動操作は未確認。

- Debug / Releaseでビルド成功、CTestの4件が成功。PNGの読み込み、全歩行タイルの透過・画像端、各方向8フレームの差異、回転後の画面基準移動、進軍の停止・再開・目的地到達も確認。
- Releaseで5,000体・10,000体・WARPの追加起動テストが成功。
- 起動テストは各5フレームで描画領域の縮小・復元、Camera変更、Scene再転送、画像読み戻しを実施。DX12デバッグレイヤーは有効で、警告・エラーは0件。
- Blender製素材の戦場と8方向×2陣営の素材確認画像を目視した。仮素材への切り替え、10,000体表示、WARPの追加起動テストも成功。コンテンツ領域は1920×1080へ更新した。
- 16フレームの進軍・回転テストは10,000体とWARPでも成功。方位22.5°の歩行確認画面、方位90°の仮素材描画も成功。回転した戦場と歩行確認画面を目視し、DX12警告・エラーは0件。
- 実際のマウス・キーボード操作の手動確認と長時間稼働・性能測定は未実施。
- Blenderの素材生成スクリプトを実行し、`py_compile`も成功。WindowsネイティブC++の変更はCMake / CTestで検証した。`package.json`はなく、`npm run build`は対象外。
